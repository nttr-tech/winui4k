{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.kt|*.kts) ./gradlew.bat spotlessApply \"-PspotlessIdeHook=$f\" --quiet >/dev/null 2>&1 || true ;; esac",
            "timeout": 120,
            "statusMessage": "Formatting with Spotless..."
          }
        ]
      }
    ]
  }
}
